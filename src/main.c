#include "constants.h"
#include "book.h"

#include <stdlib.h>

int main(int argc, char *argv[]){
    /* Main parses CLI arguments, outputs errors and launches the processing code.
    DTableUtil is comtrolled by commands, either input interactively to CLI
    or saved as a script (called template here) 
    */

    // if no arguments are provided, display version info and propose help

    // if only a filename is provided, ask whether to start interactive mode or to open a template

    // open a template file & look if there's the needed template
    // if there's none, complain

    // read the table to a string buffer

    // parse the strings, fill a 3D array of floats

    // if the commands refer to nonexistent tables or columns, complain

    // quit
    return 0;
}